apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress
  namespace: rook-ceph
spec:
  mode: one
  selector:
    labelSelectors:
      app: nodereader
  stressors:
    cpu:
      workers: 1
      load: 50
